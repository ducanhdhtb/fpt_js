function checkFormNumber(number){
    if(number == 1){
        return true;
    }else{
        var x; // x run froms 2
        var y; // y run from 1
        var pow = Math.pow(x,y);// x^y;
        if(Math.pow(x,y) == number){
            return true;
        }

    }
}
checkFormNumber(49);
// var number = [];
function sumNumberArray(){
    
}